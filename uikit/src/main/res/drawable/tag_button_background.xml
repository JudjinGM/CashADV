<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/>
    <corners android:radius="10dp"/> <!-- Закругление углов -->
    <stroke android:width="2dp" android:color="@color/tag_button_selector"/>  <!-- Цвет обводки (прозрачный) -->
</shape>
